## scripts/
- `browser_use.py` — When to use: making a script that uses the browser
- `check_balance.py` — When to use: at beginning and end of session
- `send_mail.py` — When to use: sending an email
- `sync_schedule.py` — When to use: after updating the calendar schedule
- `sync_secrets.py` — When to use: after modifying .env
